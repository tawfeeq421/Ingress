apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-volume
  labels:
    app: my-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resorces:
    requests:
      storage: 3Gi

