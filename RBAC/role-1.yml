apiVersion: rabc.authorization.k8s.io/v1
kind: Role
metadata:
  name: pod-reader
  namespace: test
  labels:
    author: Tawfeeq
rules:
  - apiGroup: [""]
    resources: ["pods"]
    verbs: ["get", "list","watch"]

